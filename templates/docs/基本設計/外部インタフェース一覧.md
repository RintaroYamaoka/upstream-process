# 外部インタフェース一覧 — <プロジェクト名>

> 記入要領: 契約は相手の文書でなく**相手の実挙動**で確認する。実物確認 = 実レスポンスを見た・
> レート制限に当たってみた・認証を実際に通した。確認していない行は課題管理表へ「観測待ち」。

| IF-ID | 相手システム | 方向 | 形式 | 認証 | 主なデータ項目(用語集の語で) | 鮮度・制限 | 実物確認(日付・方法) |
|---|---|---|---|---|---|---|---|
| IF-01 |  | 取得/送信/双方向 | REST 等 |  |  | レート制限・更新頻度 | **未確認 → Q-xx** |

## 各IFの実レスポンス記録
<!-- 実際に叩いたレスポンスのサンプルを貼る(秘匿情報はマスク)。文書上の仕様と実挙動の差分があればここに -->

### IF-01(確認日: YYYY-MM-DD)

```json
```

## 前提条件・仮定事項(AI の申告転記欄)
