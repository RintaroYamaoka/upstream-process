# ベット票 BET-XX — <一行でベットの名前> 🟡

> `iteration-design` ① の出力。**この 1 枚を超えて設計を書かない**(薄いベットは殺せる)。
> born-未追認・現行仕様と別 dir・出荷しても本番の実測が棄却条件に当たるまで 🟡 のまま。
> **配置: `docs/bets/<BET-slug>/bet.md`。派生する作業依頼書も同じフォルダに入れる**
> (幹 `docs/current-spec/` やフェーズフォルダに出さない)。⑤で殺すときはこのフォルダごと消す = 散らばらない。
> **AI と壁打ちで起票する**: 課題を渡していれば AI が課題＋事実から 6 欄の草案を埋め、リードが詰めて
> **最後に明示的に確定(born-追認)**する。避けるのは両端 —「空欄で丸投げ」と「AI 草案のまま無追認で流す」。

- **起票**: YYYY-MM-DD / <lead>(壁打ち相手 AI: 可)
- **状態**: 🟡 暫定(未証明) ← 🟢昇格は④bの実測後 / ⑤棄却は棄却条件ヒット時
- **確定**: YYYY-MM-DD <lead> 本人確定(born-追認) ← これが無い間は草案。ベットとして成立しない
- **関連課題**: Q-XX(課題管理表の 🔴 行)

---

## 1. 解こうとしている問題(なぜ今これか)
<誰のどの痛みか。事実(実測)で書く。AI が草案を出してよい欄。>

## 2. 今回の賭け
<採る方針を 1 つ。「たぶんこれで問題が解ける」という推測。**枠組みはリードが握る** —
AI 草案が違うと感じたら差し替える。それが壁打ち。>

## 3. 棄却条件(先に書く)
<何が観測されたら、この賭けは間違いか。ここが空欄なら賭けではなく願望。観測可能な文にする。>
- 例)出荷 2 週間で <指標> が <閾値> を超えなければ棄却

## 4. 測定の場所
<棄却条件を **いつ・どの実測で** 測るか。書かないと証明が永久に来ない。>
- いつ: <出荷後 N 日 / 次サイクル頭 …>
- 何を: <本番指標・ログ・利用データ・実挙動。どのダッシュボード/クエリ/画面か>
- 誰が読む: <実測を棄却条件に照らす判断者(妥当性の判定は人間・upstream)>

## 5. appetite
<これに払う時間の上限。見積りでなく「いくらの価値があるか」。超えたら④bで再判断を強制。>

## 6. 実装者が決めてはいけない 1 点
<幹(現行仕様)に触れる不可逆。ここはリードが②の作業依頼書で自分で決める。
それ以外の空白は「あなたが決めること」として明示的に委譲する。>

---

## 派生した作業依頼書(②)
> WO は**このベットフォルダ内**に置く(`docs/bets/<BET-slug>/WO-XX-<slug>.md`)。ベットフォルダの外に出さない。
- WO-XX: <…> → 同フォルダ `WO-XX-<slug>.md`(様式 = `templates/docs/bets/work-order.md`)

## 決着(④b で埋める)
- [ ] 🟢 証明: YYYY-MM-DD 実測 <…> が棄却条件を満たさず正のシグナル → 幹の <セクション> の `🟡BET-XX` 印を外した
- [ ] ⑤ 棄却: YYYY-MM-DD 棄却条件ヒット <…> → ベットを殺した(このフォルダごと破棄。出荷済みなら撤去 delta を④aで幹に追認し `🟡BET-XX` 印を外す)
- [ ] ⌛ appetite 超過で再判断: YYYY-MM-DD → <再ベット / 撤退>
