---
name: basic-design
description: 要件定義完了後の基本設計(外部設計)を AI との壁打ちで行う skill。システム構成図(データ所有者の実物確認つき)・テーブル定義書・先行試作・画面遷移図・CRUD表・外部インタフェース一覧・責任分担表・作業依頼書を docs/基本設計/ に置く。チームメンバー(とメンバーの AI)へ作業を委託する直前の工程。詳細設計以降は bootstrap の管轄なので作らない。
---

# /basic-design — 基本設計(外部設計)

外から見える振る舞い — データ・画面・外部接続 — を設計し、**他人(人間メンバーとその AI)に渡せる状態**まで持っていく。
出力は `docs/基本設計/` の文書一式と、委託単位ごとの作業依頼書。

成果物の定義・完了基準は `references/成果物カタログ.md`、様式は `templates/docs/基本設計/` を参照する。
課題管理表は要件定義から持ち越して使い続ける。

## 進め方(順序に意味がある)

### 1. システム構成図+データ所有者表
誰がどのデータを持ち、どう取得し、どれくらい新しいか。
**完了条件は「図が描けた」ではなく「全行に実物確認の日付が入った」。**
外部 API は 30 分叩く。実データは 1 件目で見る。確認できない行は課題管理表の「観測待ち」へ。
ここを図面だけで通過すると、実装後に本番データで初めて設計誤りが判明する(最も高い授業料)。

### 2. テーブル定義書(ER図)
用語集からエンティティを起こす。各カラムに 3 注記:
**保存か導出か**(導出できる値を保存するなら理由必須 — 保存した値は嘘をつける)/
**NULL の業務上の意味**(「無い」はいつ起こるか)/ **0 と「未観測」の区別**。
AI 起草の締めの申告(自分で決めた箇所の列挙)を必ず取る。テーブル設計は補完が最も混入しやすい。

### 3. 先行試作(プロトタイプ)
**画面設計より先に**、一番細いデータ経路を 1 本通す(実データ → 中心の計算 → CLI か HTML 1 枚)。
UI・認証・管理画面は含まない。これは実装なので bootstrap の管轄で行う(この skill は発注文を書くまで)。
発注者・利用者に見せる。ここで中心仮説が覆った場合の被害はほぼゼロ。画面の後で覆ると全損。

### 4. 画面一覧・画面遷移図(先行試作が生き残ってから)
各画面の目的を「誰が・何を・30秒以内にできる」で書く。全画面が機能一覧に紐づくことを確認。
**見た目・並び・余白は描き込まない** — それは実装担当者の決定領域。ここで描き込むと相手の決定権を先取りする。

### 5. CRUD表
機能×テーブルの交差表。AI に埋めさせ、人間は交差チェックだけ読む:
どの機能からも触られないテーブル・どのテーブルも触らない機能が残っていたら、どちらかが発明か漏れ。

### 6. 外部インタフェース一覧
契約は相手の文書でなく**相手の実挙動**で確認する(実レスポンス・レート制限・認証の実確認日付)。

### 7. 責任分担表(RACI)
判断領域ごとの決定者。作業の分担ではなく**判断の分担**。
「その判断を間違えたとき、最初に気づけるのは誰か」が決定者。
安定した割当だけをここに書き、案件ごとの割当は作業依頼書の「あなたが決めること」欄が主。

### 8. 作業依頼書(委託単位ごと)
様式 `templates/docs/基本設計/作業依頼書.md`。必須 4 節:
**前提(確定済み・変えない/各行に出典)・実現してほしい振る舞い(観測可能な文)・あなたが決めること・依存する未決課題(課題番号)**。

- 「あなたが決めること」の空欄は「全部こちらが決めた」の意味になり、相手を AI と同じ位置(空白を無音で埋めるしかない位置)に置く
- 受領側の締め: 完了時に「依頼に書かれていなくて自分で決めたこと」を申告してもらい、申告欄に転記
- 依頼書は相手がそのまま**自分の AI セッションに貼る**前提で書く(自己完結・ID 付き・コピペ可能)

## 変更管理(工程完了後も効き続ける唯一の手続き)

**前提を覆すとき**(用語の再定義・テーブル変更・機能の廃止):
1. その前提を引用している**進行中の作業依頼書**を洗う(前提節の出典で grep できる)
2. 依頼先に通知する
3. それから決定を確定する

承認済みの依頼の下から前提を黙って抜くと、相手の成果物が**相手の落ち度の顔をして**壊れる。

## 工程完了判定

- [ ] データ所有者表・外部IF一覧の全行に実物確認の日付があるか
- [ ] テーブル定義の全カラムに 3 注記(保存か導出か / NULL の意味 / 0 の区別)があるか
- [ ] 先行試作は通ったか。中心仮説は生きているか
- [ ] CRUD 表の交差チェックは通ったか
- [ ] 全委託単位に作業依頼書があり、「あなたが決めること」が埋まっているか
- [ ] 未決課題に依存する依頼は、課題番号が依頼書に明記されているか

ここから先(詳細設計・実装・テスト・統合)は bootstrap の管轄。この skill の仕事は終わり。
